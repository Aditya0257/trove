# mobile/ — Flutter client (placeholder)

**Not built yet.** This folder is a deliberate placeholder.

Per `DESIGN.md` §5, the Flutter mobile app (single codebase, strong camera/scan
plugins) is a later phase. Slice 1 is backend-only.

When we build it, this folder will hold the Flutter app whose primary job is
snapping a document with the camera and sending it to `POST /api/documents`.
